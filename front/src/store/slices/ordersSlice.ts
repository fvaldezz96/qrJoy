// export const payOrderMock = createAsyncThunk('orders/payMock', async (orderId: string) => {
// const { data } = await api.post(`/orders/${orderId}/pay-mock`);
// return data as { pngDataUrl: string; code: string; signature: string };
// });